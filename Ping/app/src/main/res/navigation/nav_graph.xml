<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"

    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@id/fragmentDNS"
    >

    <fragment
        android:id="@+id/fragmentDNS"
        android:name="com.example.ping.dns_resolution.view.DnsResolution"
        android:label="fragmentDNS"
        tools:layout="@layout/activity_dns_resolution"
        >
    </fragment>
</navigation>